# FastMCP framework for building MCP servers with ChatGPT Apps support
fastmcp>=2.13.0

# HTTP client for MacroMicro API requests
requests>=2.31.0

# ASGI server for running the MCP HTTP server (used with AWS Lambda Web Adapter)
uvicorn>=0.30.0

# Environment variable management (for local development)
python-dotenv>=1.0.0
